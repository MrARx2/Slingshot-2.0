# Camera Package

Contains:

- `HovercraftCamera.cs`
- `TrackSectionSensor.cs`
- Matching Unity `.meta` files

The camera expects the Slingshot hovercraft runtime, particularly `CraftCore`, and uses track-section information produced by the Track Generator package. It also uses Unity Input System.
